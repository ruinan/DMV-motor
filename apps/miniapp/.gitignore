node_modules/
dist/
# Anchored to the miniapp root so it only ignores a root-level build output —
# NOT src/lib/ (our auth/request/i18n/devStub source lives there).
/lib/
.temp/
.rn_temp/
.swc/

# Local secrets / per-env config — never commit
.env
.env.local
.env.*.local
project.private.config.json
